include junit JAR in source dist
We recently added the junit JAR under lib so that we can checkout run tests but we fail to include it in the source dist. I ll also include servlet-api-2.4.jar in binary and source dist.
